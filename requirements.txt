# --- Web framework ---
fastapi==0.141.1
uvicorn[standard]==0.46.0
python-multipart==0.0.28

# --- Database (Postgres/Supabase) ---
SQLAlchemy==2.0.51
psycopg2-binary==2.9.10
python-dotenv==1.2.2

# --- Supabase Storage (audio file uploads) ---
supabase==2.12.0

# --- AI pipeline: speech-to-text, speaker diarization, LLM ---
faster-whisper==1.2.1
pyannote.audio==3.3.2
ollama==0.6.2
torch==2.5.1
torchaudio==2.5.1

# --- Report generation (Excel / PDF export) ---
openpyxl==3.1.5
reportlab==4.5.1

# --- Shared low-level dependencies pulled in by the above
#     (pinned to avoid resolver surprises; safe to leave unpinned
#     if you'd rather let pip pick compatible versions) ---
numpy==1.26.4
requests==2.33.1
httpx==0.28.1
pydantic==2.13.4
